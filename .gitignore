node_modules/
dist/
.next/
out/
build/
coverage/
.turbo/

# Environment — never commit real secrets. Only .env.example is tracked.
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test

*.log
npm-debug.log*
pnpm-debug.log*

.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

test-results/
playwright-report/
blob-report/

*.tsbuildinfo
.pgdata/

# Committed: contains only public build-time config, no secrets.
!apps/web/.env.production

# Generated API function bundle (built during deploy)
# Generated by scripts/build-api-function.mjs during the build.
# The package.json IS tracked, so pnpm can link the workspace package at
# install time — which runs before the build that produces index.cjs.
packages/api-bundle/index.cjs
packages/api-bundle/index.cjs.map
